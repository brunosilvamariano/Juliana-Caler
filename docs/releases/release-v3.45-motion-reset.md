# v3.45 — Motion Reset

Reset completo da camada de animação. O framework passa a usar uma única implementação GSAP/ScrollTrigger criada do zero em `js/core/motion-engine.js`. Nenhum código do Animation Pack v3.44 é reutilizado.
