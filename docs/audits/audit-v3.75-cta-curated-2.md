# Auditoria v3.75 — CTA Curated 2

Escopo: curadoria e adaptação do CTA principal do Header.

## Resultado

- `calendar` e `minimal` presentes em HTML, JS, CSS, JSON e documentação ativa;
- `conversation`, `project`, `availability` e `quote` removidos do runtime e documentação ativa;
- `sidebar-premium` e `top-dock` preservados;
- matriz Header × CTA reduzida para **4 combinações**;
- total atual reduzido para **38 variantes**;
- HTML / ARIA / assets: aprovado;
- CSS: aprovado, **0 `!important` próprio**;
- JavaScript: **14 arquivos** com sintaxe válida;
- JSON: **11 arquivos** válidos;
- Content Engine: aprovado;
- `tinycss2`: todos os arquivos CSS parseados sem erro.

## Observação visual

A validação desta release foi estrutural/técnica. A composição final das duas variantes deve ser conferida no navegador com `sidebar-premium` e `top-dock` usando as queries documentadas em `VARIANTES.md`.
